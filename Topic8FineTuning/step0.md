# Step 0

```bash
pip install tinker transformers python-dotenv
export TINKER_API_KEY=your_key_here
```

But we will use the .env

The file `sql_create_context_v4.json` is located at `Topic8FineTuning/sql_create_context_v4.json`.

